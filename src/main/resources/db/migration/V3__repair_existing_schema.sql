ALTER TABLE users ADD COLUMN IF NOT EXISTS created_at TIMESTAMP(6);
UPDATE users SET created_at = CURRENT_TIMESTAMP WHERE created_at IS NULL;
ALTER TABLE users ALTER COLUMN created_at SET NOT NULL;

ALTER TABLE resource ADD COLUMN IF NOT EXISTS created_at TIMESTAMP(6);
UPDATE resource SET created_at = CURRENT_TIMESTAMP WHERE created_at IS NULL;
ALTER TABLE resource ALTER COLUMN created_at SET NOT NULL;

ALTER TABLE reservation ADD COLUMN IF NOT EXISTS created_at TIMESTAMP(6);
UPDATE reservation SET created_at = CURRENT_TIMESTAMP WHERE created_at IS NULL;
ALTER TABLE reservation ALTER COLUMN created_at SET NOT NULL;

CREATE TABLE IF NOT EXISTS refresh_token (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    token VARCHAR(500) NOT NULL UNIQUE,
    user_id BIGINT NOT NULL REFERENCES users(id),
    expires_at TIMESTAMP(6) NOT NULL,
    revoked BOOLEAN NOT NULL
);